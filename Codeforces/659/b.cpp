#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
typedef long long int lli;
#define __ ios_base::sync_with_stdio(false);cin.tie(NULL);
int t ,n,k,l;

int getd(int u){
    int x = u%(2*k);
    if(x <k/2)return x;
    else return ((2*k)-x);
}

int main(){
    cin>>t;
    while(t--){
        cin>>n>>k>>l;
        vector<int> nums(n);
        for(auto &c:nums)cin>>c;
        int time = 0;
        for(int i = 0;i<m;i++){
            if(d+getd(u)>)
        }
    }
    return 0;
}