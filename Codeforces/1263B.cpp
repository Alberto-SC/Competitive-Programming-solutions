#include <bits/stdc++.h>
using namespace std;
int main(){
    int t,n;
    cin>>t;
    while(t--){
        cin>>n;
        vector<string> pin(n);
        vector<bool> buck[4];
        for(int i = 0;i<4;i++){
            buck[i] = vector<bool>(10,false);                
        }
        for(auto &x:pin)cin>>x;
        for(int i = 0;i<4;i++){
            
        }
    }
    return 0;
}