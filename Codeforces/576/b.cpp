#include <bits/stdc++.h>
using namespace std;
const double PI  =acos(-1);
int main(){
    double h,l,c;
    cin>>h>>l;
    cout<<fixed<<setprecision(10);
    cout<<((l*l)-(h*h))/(2*h)<<endl;
    return 0;
}